const footerConfig = {
  defaultProps: {},
  styles: {
    base: {
      display: 'flex',
      width: 'w-full',
      height: 'h-fit',
      flexDirection: 'flex-col'
    }
  }
};

export default footerConfig;
