/* import React, { type FC } from 'react';
import ListItem from '../../../lib/simpleComponents/components/UI/ListItem';

interface MessageItemProps {
  name: string;
  email: string;
  message: string;
  date: string;
  isRead: boolean;
}

const MessageItem: FC<MessageItemProps> = ({id, name, email, message, date isRead}) => {
  return <ListItemButton></ListItemButton>;
};

export default MessageItem; */
